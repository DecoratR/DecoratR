namespace DecoratR.Generator;

internal static class DecoratRGeneratorVersion
{
    // This value is replaced by the CI pipeline during release with the actual tag version.
    internal const string Value = "1.0.0";
}
